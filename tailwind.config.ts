import type { Config } from 'tailwindcss';

const config: Config = {
  content: [], // Tell Tailwind to scan no files
  theme: {
    extend: {}, // Empty theme
  },
  plugins: [], // No plugins
  // darkMode: 'class', // Keep darkMode commented for now
};
export default config;
