
// Esta página não está mais em uso e pode ser excluída,
// pois a Base de Conhecimento foi reformulada para integração com o Google Drive.
export default function KnowledgeBaseArticlePage({ params }: { params: { id: string } }) {
  return null;
}
